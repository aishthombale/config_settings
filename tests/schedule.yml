jobs.test_job_1:
  trigger: interval
  every: 1
  unit: minutes

jobs.test_job_2:
  trigger: interval
  every: 1
  unit: minutes

jobs.test_job_3:
  trigger: interval
  every: 10
  unit: seconds

jobs.test_job_4:
  trigger: interval
  every: 10
  unit: minutes